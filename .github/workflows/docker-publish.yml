name: Build & publish infra containers

on:
  push:
    branches: [ main ]
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - dockerfile: ./infra/Dockerfile_db
            image: ghcr.io/zcaudate/infra-db
          - dockerfile: ./infra/Dockerfile_expo
            image: ghcr.io/zcaudate/infra-expo
          - dockerfile: ./infra/Dockerfile_net
            image: ghcr.io/zcaudate/infra-net
          - dockerfile: ./infra/Dockerfile_redis
            image: ghcr.io/zcaudate/infra-redis
          - dockerfile: ./infra/Dockerfile_ganache
            image: ghcr.io/zcaudate/infra-ganache
          - dockerfile: ./infra/Dockerfile_redis_v5
            image: ghcr.io/zcaudate/infra-redis-5
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Workaround: https://github.com/docker/build-push-action/issues/461
      - name: Setup Docker buildx
        uses: docker/setup-buildx-action@v2

      # Login against a Docker registry except on PR
      # https://github.com/docker/login-action
      - name: Log into registry ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ matrix.image }}

      - name: Build and push Docker infra images
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
